# Future naming checklist

In one mechanical naming PR, decide and update the repository name, CMake project, target names, executable, C++ namespace, public include root, asset directory, window title, README language, package metadata, and whether `.meshgeo` remains the file extension. Update CI, install paths, tests, docs, and compatibility/migration notes together. No rename is performed during repository preparation.
